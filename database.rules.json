{
  "rules": {
    ".read": false,
    ".write": false,
    "presence": {
      "$cid": {
        ".read": "auth != null",
        "$uid": {
          ".write": "auth != null && auth.uid === $uid && $cid.matches(/^[A-Za-z0-9]{20}$/)",
          ".validate": "newData.isBoolean()"
        }
      }
    },
    "typing": {
      "$cid": {
        "$channel": {
          ".read": "auth != null && ($channel === 'messages' || $channel === 'mixed_messages' || $channel === 'staff_messages')",
          "$uid": {
            ".write": "auth != null && auth.uid === $uid && $cid.matches(/^[A-Za-z0-9]{20}$/) && ($channel === 'messages' || $channel === 'mixed_messages' || $channel === 'staff_messages')",
            ".validate": "newData.isNumber() && newData.val() <= now + 5000"
          }
        }
      }
    }
  }
}
